# Loaded when APP_ENV=testing (php artisan test / Pest).
# All real test values live in phpunit.xml <php> — this file mainly exists so
# the test suite runs cleanly on a fresh clone before .env has been created.
APP_ENV=testing
