// Application entrypoint (TypeScript).
// Built by Vite into public/build/assets/app.js and loaded in the layout.
//
// The CSS is imported here so Vite bundles it into public/build/assets/app.css.
// Keep dependencies lean — this template ships with just Blade + TypeScript
// and no UI framework.
import '../css/app.css';

export {};
